## Description 
Very simple python program that does this anv that

## Project structure
The application code can be found under `src` foldeer.
The unit-tests can be found under `tests` folder.
Project dependencies are defined in the `requirements.txt` file.

## How to use
Run `python src/application.py` in your favourite terminal.

## How to test
Run `pytest` in your favourite terminal.